apiVersion: v1
kind: ConfigMap
metadata:
  name: postfix-config
  namespace: mail-stack
data:
  main.cf: |
    compatibility_level = 3.6
    maillog_file = /dev/stdout

    # --- Identité ---
    myhostname = mail.k8s.malagasy.com
    mydomain = k8s.malagasy.com
    myorigin = $mydomain
    smtp_helo_name = smtp-out.k8s.malagasy.com

    # --- Réseau ---
    inet_interfaces = all
    inet_protocols = all
    smtp_address_preference = ipv4
    mynetworks = 127.0.0.0/8, 10.244.0.0/16, 192.168.6.0/24

    # --- Destinations ---
    mydestination = localhost
    local_recipient_maps =

    # --- RELAIS SORTANT (Vers k8s-nat-2) ---
    # On utilise l'IP LAN du NAT pour sortir
    relayhost = [192.168.6.219]:25
    smtp_tls_security_level = may
    smtp_sasl_auth_enable = no

    # --- Optimisations et DNS ---
    smtpd_peername_lookup = no
    smtpd_delay_reject = yes
    smtpd_helo_required = yes
    smtpd_data_restrictions = reject_unauth_pipelining
    smtp_host_lookup = native, dns
    lmtp_host_lookup = native, dns

    # --- Mappings MySQL ---
    virtual_mailbox_domains = proxy:mysql:/etc/postfix/mysql-virtual-mailbox-domains.cf
    virtual_mailbox_maps = proxy:mysql:/etc/postfix/mysql-virtual-mailbox-maps.cf
    virtual_alias_maps = proxy:mysql:/etc/postfix/mysql-virtual-alias-maps.cf

    # --- Transport Dovecot (LMTP) ---
    virtual_transport = lmtp:inet:dovecot-service.mail-stack.svc.cluster.local:24

    # --- Authentification SASL (Via Dovecot) ---
    smtpd_sasl_type = dovecot
    smtpd_sasl_path = inet:dovecot-service.mail-stack.svc.cluster.local:10000
    smtpd_sasl_auth_enable = yes
    smtpd_sasl_security_options = noanonymous
    smtpd_sasl_authenticated_header = yes
    broken_sasl_auth_clients = yes

    # --- TLS (Certificats Let's Encrypt) ---
    smtpd_tls_security_level = may
    smtpd_tls_cert_file = /etc/postfix/tls.crt
    smtpd_tls_key_file = /etc/postfix/tls.key
    smtpd_tls_loglevel = 1

    # --- Filtrage Antispam (Rspamd) ---
    smtpd_milters = inet:rspamd-service.mail-stack.svc.cluster.local:11332
    non_smtpd_milters = inet:rspamd-service.mail-stack.svc.cluster.local:11332
    milter_protocol = 6
    milter_default_action = accept

    # --- Restrictions de Relais ---
    smtpd_relay_restrictions = permit_mynetworks, permit_sasl_authenticated, reject_unauth_destination
    smtpd_recipient_restrictions = permit_mynetworks, permit_sasl_authenticated, reject_unauth_destination

  master.cf: |
    # Port 25 - Réception Externe (Proxy Protocol HAProxy requis)
    smtp      inet  n       -       y       -       -       smtpd
      -o smtpd_upstream_proxy_protocol=haproxy
      -o smtpd_upstream_proxy_timeout=5s

    # Port 587 - Soumission Interne (Roundcube) & Clients directs (SANS Proxy Protocol)
    submission inet n       -       y       -       -       smtpd
      -o syslog_name=postfix/submission
      -o smtpd_tls_security_level=encrypt
      -o smtpd_sasl_auth_enable=yes
      -o smtpd_upstream_proxy_protocol=
      -o smtpd_relay_restrictions=permit_sasl_authenticated,reject

    # Port 465 - SMTPS Interne (SANS Proxy Protocol)
    smtps     inet  n       -       y       -       -       smtpd
      -o syslog_name=postfix/smtps
      -o smtpd_tls_wrappermode=yes
      -o smtpd_sasl_auth_enable=yes
      -o smtpd_upstream_proxy_protocol=
      -o smtpd_relay_restrictions=permit_sasl_authenticated,reject

    # Services internes obligatoires
    pickup    unix  n       -       y       60      1       pickup
    cleanup   unix  n       -       y       -       0       cleanup
    qmgr      unix  n       -       y       300     1       qmgr
    tlsmgr    unix  -       -       y       1000    1       tlsmgr
    rewrite   unix  -       -       y       -       -       trivial-rewrite
    bounce    unix  -       -       y       -       0       bounce
    defer     unix  -       -       y       -       0       bounce
    proxymap  unix  -       -       y       -       -       proxymap
    proxywrite unix -       -       y       -       1       proxymap
    smtp      unix  -       -       y       -       -       smtp
    relay     unix  -       -       y       -       -       smtp
    showq     unix  n       -       y       -       -       showq
    error     unix  -       -       y       -       -       error
    retry     unix  -       -       y       -       -       error
    discard   unix  -       -       y       -       -       discard
    local     unix  -       -       y       -       -       local
    virtual   unix  -       -       y       -       -       virtual
    lmtp      unix  -       -       y       -       -       lmtp
    anvil     unix  -       -       y       -       1       anvil
    scache    unix  -       -       y       -       1       scache
    postlog   unix-dgram n  -       y       -       1       postlogd

  mysql-virtual-mailbox-domains.cf: |
    user = mailuser
    password = __DB_PASSWORD__
    hosts = mariadb-service.mail-stack.svc.cluster.local
    dbname = mailserver
    query = SELECT 1 FROM virtual_domains WHERE name='%s'

  mysql-virtual-mailbox-maps.cf: |
    user = mailuser
    password = __DB_PASSWORD__
    hosts = mariadb-service.mail-stack.svc.cluster.local
    dbname = mailserver
    query = SELECT 1 FROM virtual_users WHERE email='%s' AND enabled=1

  mysql-virtual-alias-maps.cf: |
    user = mailuser
    password = __DB_PASSWORD__
    hosts = mariadb-service.mail-stack.svc.cluster.local
    dbname = mailserver
    query = SELECT destination FROM virtual_aliases WHERE source='%s'
